<% content_for :title, @term.name %>

<div class="container mx-auto px-2 pb-8">
  <div class="flex justify-between items-center mb-6">
    <div>
      <h1 class="text-2xl font-bold text-on-surface"><%= @term.name %></h1>
      <p class="text-on-surface-variant text-sm"><%= @term.uid %></p>
    </div>
    <%= link_to "Back to Terms", admin_terms_path, class: "m3-btn-outlined px-4 py-2 rounded text-sm cursor-pointer inline-block" %>
  </div>

  <div>
    <h2 class="text-xl font-semibold mb-4 text-on-surface">Courses (<%= @term.courses.count %>)</h2>
    <% if @courses.any? %>
      <div class="m3-card shadow-sm rounded-xl overflow-hidden">
        <div class="overflow-x-auto">
          <table class="min-w-full divide-y divide-outline-variant">
            <thead class="bg-surface-container-high">
              <tr>
                <th scope="col" class="px-6 py-3 text-left text-xs font-medium text-on-surface-variant uppercase tracking-wider">Course ID</th>
                <th scope="col" class="px-6 py-3 text-left text-xs font-medium text-on-surface-variant uppercase tracking-wider">Name</th>
                <th scope="col" class="px-6 py-3 text-left text-xs font-medium text-on-surface-variant uppercase tracking-wider">Instructor</th>
                <th scope="col" class="px-6 py-3 text-left text-xs font-medium text-on-surface-variant uppercase tracking-wider">Location</th>
              </tr>
            </thead>
            <tbody class="divide-y divide-outline-variant">
              <% @courses.each do |course| %>
                <tr>
                  <td class="px-6 py-4 whitespace-nowrap text-sm font-medium text-on-surface"><%= course.crn %></td>
                  <td class="px-6 py-4">
                    <div class="text-sm text-on-surface"><%= course.title %></div>
                    <div class="text-xs text-on-surface-variant"><%= course.section_number %></div>
                  </td>
                  <td class="px-6 py-4 whitespace-nowrap text-sm text-on-surface">
                    <% if course.faculties.any? %>
                      <%= safe_join(course.faculties.map { |f| link_to f.full_name, admin_faculty_path(f), class: "text-primary hover:underline" }, ", ") %>
                    <% else %>
                      TBA
                    <% end %>
                  </td>
                  <td class="px-6 py-4 whitespace-nowrap text-sm text-on-surface">
                    <%= course.meeting_times.map { |mt| mt.building_room }.compact.uniq.join(", ").presence || "TBA" %>
                  </td>
                </tr>
              <% end %>
            </tbody>
          </table>
        </div>
        <% if @courses.respond_to?(:total_pages) %>
          <div class="px-6 py-4 border-t border-outline-variant bg-surface-container-low">
            <%= paginate @courses %>
          </div>
        <% end %>
      </div>
    <% else %>
      <p class="text-on-surface-variant">No courses in this term.</p>
    <% end %>
  </div>
</div>
